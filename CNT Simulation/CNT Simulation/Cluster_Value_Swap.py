# array_1 = [a,b]
# array_2 = [c,d]
# arrays = [array_1, array_2]
# def replace_array(replace_name, replace_value, arrays):
#     for array in arrays:
#         for variable in array:
#             if replace_name == 'a':
                
def sum_values(array):
    return sum(array)